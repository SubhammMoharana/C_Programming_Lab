//write input element in array and reomve all the duplicate element
#include<stdio.h>
int main()
{
    int arr[],n;
    //input
    printf("Enter the size:",n);
    scanf("%d",&n);
    //inputing elements
    printf("Enter the elements");
    for(int i = 0; i<n; i++){
        scanf("%d",arr[i]);
    }
    //logic
    
    return 0;
}